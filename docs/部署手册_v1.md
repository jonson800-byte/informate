# Informate MVP 部署手册 v1（T15）

> 适用版本：MVP（行业工作助手 + 生图 + 积分计费 + 合规审查）
> 项目根目录：`~/Desktop/Informate Project/`
> 文档依据：`docs/产品设计/PRD_v1.md`、`artifacts/02_design/技术方案_v1.md`、`scripts/dev_start.sh`（实测固化流程）
> 最后校验：2026-08-07（T13 集成联调冒烟通过）

---

## 1. 架构与端口一览

### 1.1 组件拓扑

```
┌──────────────┐   /api (proxy)   ┌──────────────────┐
│  前端        │ ───────────────▶ │  后端 backend    │
│  React+Vite  │ :5173            │  Node+TS+Fastify │ :8080
│  (三栏工作台) │ ◀─────────────── │  +SQLite         │
└──────────────┘    JSON/SSE      └────────┬─────────┘
                                           │
                     ┌─────────────────────┼─────────────────────┐
                     ▼                     ▼                     ▼
          ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
          │ 合规服务         │  │ Hermes api_server│  │ 火山方舟 Seedream │
          │ Python FastAPI   │  │ 真实对话(可选)    │  │ 真实生图(可选)    │
          │ :9100            │  │ :8647 (外部进程)  │  │ 外部 API          │
          └──────────────────┘  └──────────────────┘  └──────────────────┘

  任务队列：REDIS_URL 已配置且安装 bullmq → Redis+BullMQ；否则内存 FIFO（MVP 单机形态）
  记忆：Hindsight :9177（可选外部服务，不可用时记日志降级）
```

### 1.2 端口一览

| 端口 | 服务 | 目录 | 开发启动 | 生产启动 |
|:--|:--|:--|:--|:--|
| **8080** | 后端 API（Fastify + SQLite） | `backend/` | `npm run dev`（tsx watch） | `npm run build && npm run start`（node dist/index.js） |
| **9100** | 合规服务（FastAPI） | `services/compliance/` | `.venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 9100` | 同上（uvicorn 由 pm2 托管） |
| **5173** | 前端开发服务器（Vite，`/api` 代理到 8080） | `frontend/` | `npm run dev` | **不启动**，见下 |
| 8647 | Hermes api_server（可选，真实对话用） | 外部 | 外部启动 | 外部启动 |
| 9177 | Hindsight（可选，记忆服务） | 外部 | 外部启动 | 外部启动 |
| 6379 | Redis（可选，BullMQ 队列） | 外部 | 外部启动 | 外部启动 |

### 1.3 生产环境前端托管（重要）

**开发**：Vite dev server（:5173），`/api` 请求由 vite proxy 转发到 :8080。
**生产**：**不跑 Vite dev server**。构建静态产物后用任意静态服务器托管：

```bash
cd frontend
npm run build        # 产物输出到 frontend/dist/
# 托管 dist/（任选其一）：
npx serve dist -l 80                     # 简单静态托管
# 或 nginx 配置示例：
#   server {
#     listen 80;
#     root /opt/informate/frontend/dist;
#     location /api/ { proxy_pass http://127.0.0.1:8080; }   # 转发后端
#     location / { try_files $uri /index.html; }             # SPA 路由回退
#   }
```

> 注意：静态托管时 `/api` 必须反向代理到 :8080（生产不再走 Vite proxy）。SSE（chat 流式）经 nginx 需关闭缓冲：`proxy_buffering off;`，否则对话流式输出会卡顿。

---

## 2. 环境准备

### 2.1 基础依赖

| 依赖 | 版本要求 | 验证命令 | 说明 |
|:--|:--|:--|:--|
| Node.js | **≥ 18**（package.json engines 要求 ≥20，建议 20 LTS） | `node -v` | 后端运行环境 |
| npm | ≥ 9 | `npm -v` | 随 Node 安装 |
| Python | **3.11** | `python3 --version` | 合规服务 |
| SQLite | 3.x（内置，无需安装） | — | 由 better-sqlite3 驱动，无需独立进程 |

### 2.2 安装步骤（全新机器）

```bash
cd ~/Desktop/Informate\ Project

# 1) 后端依赖
cd backend && npm install && cd ..

# 2) 前端依赖
cd frontend && npm install && cd ..

# 3) 合规服务（Python 3.11 虚拟环境）
cd services/compliance
python3.11 -m venv .venv                 # 若系统 python3 已是 3.11 可直接 python3
.venv/bin/pip install -r requirements.txt
cd ../..
```

### 2.3 生产环境可选/必备依赖

| 组件 | 必要性 | 说明 |
|:--|:--|:--|
| Redis | **可选** | 配置 `REDIS_URL` 且 `cd backend && npm i bullmq` 后任务队列走 Redis+BullMQ；不配则回落内存 FIFO（MVP 单机可用，重启丢排队任务） |
| 火山方舟 API Key | 真实生图必备 | 设置 `VOLC_ARK_API_KEY` 后生图走真实 Seedream；不设置则返回 mock 占位图（联调/演示可用，**上线必须真实**） |
| Hermes api_server | 真实对话必备 | 设置 `HERMES_MODE=real` + `HERMES_API_BASE` + `HERMES_API_KEY` 后 Chat 转发真实 api_server（:8647）；默认 `mock` 为本地占位回复（联调用） |
| Hindsight | 可选 | `HINDSIGHT_API_BASE` 指向 :9177；不可用时记忆写入记日志降级，不影响主流程 |
| pm2 | 生产建议 | 进程守护/自动重启，见第 4 章 |

---

## 3. 数据库初始化

### 3.1 首次初始化

```bash
cd ~/Desktop/Informate\ Project/backend
npm run migrate    # 建表（SQLite WAL 模式）
npm run seed       # 写入种子数据：示例租户 + 账号 + 价格配置 + 场景部署
```

- 数据文件：`backend/data/informate.db`（同目录另有 `-wal` / `-shm` 为 WAL 辅助文件，备份/恢复时一并处理）
- 种子账号：

| 账号 | 密码 | 角色 | 用途 |
|:--|:--|:--|:--|
| owner | owner123 | 租户主账号（医美示例租户） | 工作台/管理 |
| employee | emp123 | 员工账号 | 工作台 |
| admin | admin123 | 平台管理员（独立 admin 表） | 管理后台 |

- 种子价格配置：工作助手 10 积分/会话（含 20 轮，超轮 1 积分/轮，上限 50 轮）；生图对外 20 积分、实扣 15；充值档 100=1100 / 500=6000 / 2000=25000；部署费 ¥500/场景一次性（首单免）。
- ⚠️ **必须在 migrate 之后才能启动 backend**。若 backend 已在跑且登录报 `UNAUTHORIZED 账号或密码错误`，多半是旧实例持空库连接 → `kill` 旧进程重启（见第 6 章排障）。

### 3.2 一键重置（开发环境）

```bash
bash scripts/dev_start.sh --reset-db   # 删库 + migrate + seed，回到干净测试数据
```

### 3.3 备份 / 恢复

**备份 = 复制文件**（WAL 模式下推荐用 sqlite 在线备份或先停服再复制，避免拷到未合并的 -wal）：

```bash
# 方式 A：停服备份（最稳，推荐例行备份）
cd ~/Desktop/Informate\ Project/backend
sqlite3 data/informate.db ".backup '/backup/informate_$(date +%Y%m%d_%H%M%S).db'"
# 或简单复制（需先停服，避免 -wal 未落盘）
# cp data/informate.db* /backup/

# 方式 B：不停服在线备份（better-sqlite3 支持，直接复制三个文件亦可但 -wal 可能滞后）
cp data/informate.db data/informate.db-wal data/informate.db-shm /backup/
```

> 建议备份频率：每日一次 + 每次发版前一次。备份保留最近 7 份。

**恢复 = 停服替换 + 重启**：

```bash
# 1) 停掉 backend（pm2 或 kill，见第 4 章）
# 2) 替换数据文件（连同 -wal/-shm 一起删掉旧的，防止脏 WAL 覆盖新数据）
cd ~/Desktop/Informate\ Project/backend
rm -f data/informate.db data/informate.db-wal data/informate.db-shm
cp /backup/informate_YYYYMMDD_HHMMSS.db data/informate.db
# 3) 重启 backend，验证登录与 /health
```

---

## 4. 启动 / 停止 / 重启

### 4.1 开发环境（一键脚本）

```bash
cd ~/Desktop/Informate\ Project
bash scripts/dev_start.sh          # 依次拉起 compliance:9100 → 数据库 → backend:8080 → frontend:5173（已运行的服务自动跳过）
bash scripts/dev_start.sh --reset-db   # 重置数据库后启动
bash scripts/dev_smoke.sh         # 冒烟：登录→工作台→会话→chat SSE→生图→充值，6 步全链路验证
```

开发日志：`/tmp/informate_backend.log`、`/tmp/informate_frontend.log`、`/tmp/informate_compliance.log`。
开发停止：`lsof -ti :8080 | xargs kill`（8080/9100/5173 各自处理），或 pm2 托管后统一管理。

### 4.2 生产环境（pm2 建议）

```bash
npm i -g pm2

# 首次构建（backend TS 编译 + frontend 静态产物）
cd ~/Desktop/Informate\ Project/backend && npm run build && cd ..
cd frontend && npm run build && cd ..

# 启动（在项目根目录执行，pm2 配置文件见下方 ecosystem.config.js）
pm2 start ecosystem.config.js
pm2 save                      # 保存进程列表，开机自启：pm2 startup
```

**ecosystem.config.js 示例**（放项目根目录）：

```js
module.exports = {
  apps: [
    {
      name: 'informate-backend',
      cwd: '/Users/zhangyihui/Desktop/Informate Project/backend',
      script: 'dist/index.js',          // npm run build 产物
      interpreter: 'node',
      instances: 1,                      // 单实例（better-sqlite3 单进程，勿开 cluster）
      autorestart: true,
      max_restarts: 10,
      env: {
        NODE_ENV: 'production',
        PORT: '8080',
        DB_PATH: 'data/informate.db',
        JWT_SECRET: '<生产强随机密钥>',   // 必须注入！见第 5 章
        ARTIFACTS_DIR: 'data/artifacts',
        // SEEDREAM_MODE: 'mock',        // 真实生图不要设置此项（默认 real）
        // VOLC_ARK_API_KEY: '<火山方舟Key>',
        // REDIS_URL: 'redis://localhost:6379',
        HERMES_MODE: 'real',             // 真实对话
        HERMES_API_BASE: 'http://127.0.0.1:8647',
        HERMES_API_KEY: '<api_server Key>',
        // HINDSIGHT_API_BASE: 'http://localhost:9177',
        COMPLIANCE_BASE_URL: 'http://127.0.0.1:9100',
      },
      out_file: '/tmp/informate_backend.log',
      error_file: '/tmp/informate_backend_err.log',
      merge_logs: true,
    },
    {
      name: 'informate-compliance',
      cwd: '/Users/zhangyihui/Desktop/Informate Project/services/compliance',
      script: '.venv/bin/python',
      args: '-m uvicorn main:app --host 127.0.0.1 --port 9100',
      autorestart: true,
      out_file: '/tmp/informate_compliance.log',
      error_file: '/tmp/informate_compliance_err.log',
    },
    // 前端：生产不用 pm2 托管 dev server，静态 dist/ 交给 nginx/serve（见 1.3）
  ],
}
```

> ⚠️ 不用 `script: 'npm' args: 'run start'` 的写法会引入 npm 包装进程；直接指向 `dist/index.js` 更稳。若坚持 `pm2 start --name informate-backend 'npm run start'`，请 `pm2 start ecosystem.config.js` 二选一，勿混用。

### 4.3 停止 / 重启 / 状态

```bash
pm2 stop informate-backend            # 停止
pm2 restart informate-backend         # 重启（加载新代码/新环境变量）
pm2 reload informate-backend          # 零停机重载（单实例下同 restart）
pm2 status                            # 查看所有进程状态
pm2 logs informate-backend            # 实时日志
pm2 startup && pm2 save               # 开机自启
```

后端支持优雅退出：SIGINT/SIGTERM 时先 `app.close()` 再退出，pm2 的 stop/restart 默认发 SIGINT，无需额外配置。

---

## 5. 环境变量清单

后端读取 `.env`（`backend/` 下，模板见 `backend/.env.example`）与进程环境变量；进程环境变量优先。

| 变量 | 默认值 | 说明 |
|:--|:--|:--|
| `PORT` | `8080` | 后端监听端口 |
| `DB_PATH` | `data/informate.db`（相对 backend/） | SQLite 数据文件路径；测试用 `:memory:` |
| `JWT_SECRET` | 开发 `informate-dev-secret-change-me` | JWT 签名密钥。**生产必须注入强随机值**：`NODE_ENV=production` 且未设置时直接拒绝启动；开发模式会打印警告 |
| `JWT_EXPIRES_IN` | `7d` | JWT 有效期（可选） |
| `ARTIFACTS_DIR` | `data/artifacts`（相对 backend/） | 生图产物落盘目录 |
| `SEEDREAM_MODE` | 未设置（由 Key 自动判定） | `mock` 强制占位图模式；不设且配了 Key 则走真实 Seedream |
| `VOLC_ARK_API_KEY` | 空 | 火山方舟 API Key。**设置后生图走真实模式**；不设置返回 mock 占位图（上线必须设置） |
| `REDIS_URL` | 空 | 如 `redis://localhost:6379`。设置且已 `npm i bullmq` → Redis+BullMQ 队列；否则内存 FIFO（重启丢排队任务） |
| `HERMES_MODE` | `mock` | `mock`=本地占位流式回复（联调）；`real`=转发真实 Hermes api_server（上线必须 `real`） |
| `HERMES_API_BASE`（部署任务书又称 HERMES_BASE_URL，代码实际读取前者） | `http://127.0.0.1:8647` | Hermes api_server 地址；`HERMES_MODE=real` 时生效 |
| `HERMES_API_KEY` | `spike-userid-test-key` | api_server 调用密钥；`HERMES_MODE=real` 时使用 |
| `HINDSIGHT_API_BASE` | `http://localhost:9177` | Hindsight 记忆服务地址；不可用时记忆写入降级（记日志） |
| `COMPLIANCE_BASE_URL` | `http://127.0.0.1:9100` | 合规服务地址；不可用时 Chat/生图 fail-closed 503（医美红线） |
| `PUBLISH_REVIEW_HINT` | 内置缺省文案 | 合规服务（compliance，非 backend）环境变量：医美文案发布审查提示，启动覆盖 `GET /hint` 返回的缺省文案（运行时可用 `PUT /hint` 持久化到 `data/publish_hint.json`） |
| `NODE_ENV` | 未设置 | 设 `production` 强制 JWT_SECRET 校验（pm2 env 中设置） |

生产必配 4 项：`NODE_ENV=production`、`JWT_SECRET`（强随机）、`VOLC_ARK_API_KEY`（真实生图）、`HERMES_MODE=real` + `HERMES_API_BASE` + `HERMES_API_KEY`（真实对话）。

生成强随机密钥：

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

---

## 6. 日志与排障

### 6.1 日志位置

| 服务 | 日志文件 | 查看 |
|:--|:--|:--|
| backend（开发 dev_start.sh） | `/tmp/informate_backend.log` | `tail -f /tmp/informate_backend.log` |
| backend（pm2） | `/tmp/informate_backend.log` / `_err.log` | `pm2 logs informate-backend` |
| compliance | `/tmp/informate_compliance.log` | `tail -f /tmp/informate_compliance.log` |
| frontend（开发） | `/tmp/informate_frontend.log` | `tail -f /tmp/informate_frontend.log` |

### 6.2 健康检查

```bash
curl -s http://127.0.0.1:8080/health        # backend，期望 200 ok
curl -s http://127.0.0.1:9100/health        # compliance，期望 200 ok
curl -s http://127.0.0.1:5173/              # frontend（开发），期望 HTML
```

### 6.3 常见问题速查表

| 症状 | 可能原因 | 处理 |
|:--|:--|:--|
| 启动 backend 报 `EADDRINUSE` / 端口被占 | 8080 被旧实例/其他进程占用 | `lsof -i :8080` 找到 PID → `kill <PID>` 后重启 |
| 登录报 `UNAUTHORIZED 账号或密码错误` | 旧 backend 实例持空库连接（早于 migrate 启动） | `lsof -i :8080` 杀掉旧 PID → 重启 backend（DB 文件本身正常，勿删库） |
| `SQLITE_BUSY` / DB 锁错误 | 多进程同时写同一个 db 文件（如误开 cluster 多实例） | 确认 backend 单实例运行；better-sqlite3 单进程 + WAL，勿 `instances > 1` |
| 启动日志 `开发模式使用默认 JWT_SECRET` 警告 | 生产未注入 JWT_SECRET（或 NODE_ENV 未设 production） | 注入强随机 `JWT_SECRET` + `NODE_ENV=production`；否则任何人都可用已知默认密钥签发 admin token |
| Chat/生图返回 `503`（合规服务不可用） | `COMPLIANCE_BASE_URL` 指向的 :9100 没起来 | 启动 compliance（`bash scripts/dev_start.sh` 或 pm2）；确认 `curl :9100/health` 通过。合规 fail-closed 是医美红线设计 |
| 生图返回 `402`（余额不足） | 火山方舟账户欠费/额度耗尽 | 火山控制台充值/续额度；临时验证可 `SEEDREAM_MODE=mock` 切占位图，但**上线必须真实** |
| 生图一直返回占位图 | 未设 `VOLC_ARK_API_KEY`，或 `SEEDREAM_MODE=mock` 残留 | 注入真实 Key；检查环境变量是否被 .env 覆盖 |
| 对话是占位回复 | `HERMES_MODE=mock` 或 api_server 未启动 | 设 `HERMES_MODE=real`；确认 `curl http://127.0.0.1:8647/health` 可达、Key 正确 |
| `REDIS_URL 已设置但未安装 bullmq` 警告 | 配了 Redis 但没装 bullmq | `cd backend && npm i bullmq`（不装则回落内存 FIFO，功能可用但重启丢排队任务） |
| 前端页面空白/接口 404 | 静态托管未代理 `/api`，或 SPA 路由未回退 | nginx 配 `location /api/ { proxy_pass http://127.0.0.1:8080; }` + `try_files $uri /index.html` |
| SSE 流式对话卡顿/不实时 | 反向代理缓冲了 SSE | nginx 加 `proxy_buffering off;` |

---

## 7. 回滚

> ⚠️ 当前项目目录尚未初始化 git（`git rev-parse` 返回 not a git repository）。**上线前请先 `git init` 并提交基线**，否则代码回滚无从谈起。

### 7.1 代码回滚（git）

```bash
cd ~/Desktop/Informate\ Project
git log --oneline -10                 # 找到目标版本
git checkout <目标commit> -- backend frontend services   # 回退到指定版本（保留提交历史）
# 或硬回退：git reset --hard <目标commit>

# 重新构建 + 重启
cd backend && npm install && npm run build && cd ..
cd frontend && npm install && npm run build && cd ..
pm2 restart informate-backend informate-compliance
```

> 回退后建议跑 `bash scripts/dev_smoke.sh`（或生产环境最小冒烟：登录 → chat → 生图）确认链路。

### 7.2 数据回滚（DB 恢复备份）

```bash
# 1) 停服
pm2 stop informate-backend
# 2) 替换 DB（连同 WAL 文件一起清掉，防止脏 WAL 污染）
cd ~/Desktop/Informate\ Project/backend
rm -f data/informate.db data/informate.db-wal data/informate.db-shm
cp /backup/informate_YYYYMMDD_HHMMSS.db data/informate.db
# 3) 重启
pm2 start informate-backend
curl -s http://127.0.0.1:8080/health   # 验证
```

### 7.3 回滚决策要点

- **只回代码**：数据 schema 未变（仅逻辑 bug）→ 只做 7.1。
- **只回数据**：误操作/脏数据 → 只做 7.2。
- **代码+数据都回**：发版后 schema 变更/数据损坏 → 先 7.1 回代码，再 7.2 回数据（顺序：代码 → 数据 → 重启）。
- 回滚后必须验证：`/health`、登录、会话计费、生图。

---

## 8. 上线前清单

- [ ] **真实密钥注入**：`VOLC_ARK_API_KEY`（真实生图）、`HERMES_MODE=real` + `HERMES_API_BASE` + `HERMES_API_KEY`（真实对话）已配置；`HERMES_MODE` 不为 `mock`
- [ ] **`JWT_SECRET` 必改**：注入 ≥48 字节强随机值，`NODE_ENV=production`；确认启动日志无「使用默认 JWT_SECRET」警告
- [ ] **DeepSeek/模型成本重算定价**：外部模型涨价后，按「工作助手 10 积分/会话（20 轮）、超轮 1 分/轮、生图实扣 15」重算毛利，必要时更新价格配置（`backend` 价格表/seed 脚本）并**重新 seed 或 SQL 更新 effective_at 为当前时间**
- [ ] **合规备案**：医美行业文案需《医疗广告审查证明》方可投放（FR-209），`GET http://127.0.0.1:9100/hint` 提示文案确认；合规服务 fail-closed 验证（停掉 :9100 → Chat 应 503）
- [ ] **AI 标识**：按《生成式人工智能服务管理暂行办法》要求，AI 生成内容（对话回复、生图产物）需显著标识；前端/产物水印确认已实现
- [ ] **管理后台账号**：admin/admin123 为种子默认密码，**上线必须改密**（或禁用种子账号另建管理员）；确认 owner/employee 密码策略
- [ ] **数据库备份**：上线前完整备份 `backend/data/informate.db` 并存档
- [ ] **日志与监控**：确认 pm2 日志落盘、`/health` 可被外部监控轮询
- [ ] **静态托管**：frontend `dist/` 已部署，`/api` 代理与 SPA 回退、SSE 关缓冲均验证
- [ ] **端口放行**：8080/9100 仅内网可达（不直接暴露公网），前端经 80/443 对外

---

## 9. 零号客户试点准备清单（医美）

目标：零号客户（医美机构）从开户到打样可复制，**打样指标：周活 ≥ 60% / 任务完成率 ≥ 70% / 续费意向 ≥ 80%**。

### 9.1 流程（顺序执行）

1. **开户**：管理后台创建客户租户（或复用种子租户流程）→ 分配行业=医美、场景=行业工作助手 + 生成图片
2. **试用**：开通试用额度（默认 `trial` 状态）→ 引导客户走一遍「工作助手会话 → 生图 → 充值」全链路，确认无 503/占位图
3. **充值**：客户确认效果后首次充值（档位 100=1100 / 500=6000 / 2000=25000 积分）；部署费 ¥500/场景一次性（首单免）
4. **员工账号**：为客户创建员工账号（employee 角色，密码按客户规范设置并交付），按需分配权限
5. **培训 15 分钟**：现场培训标准动作——
   - 打开工作台 → 新建会话（消耗 10 积分/会话）
   - 输入医美场景问题（如话术/案例/合规提醒），演示 SSE 流式回复与 20 轮内免费、超轮计费
   - 演示生图（提示词 + 合规前置检查），下载产物
   - 演示充值流程与积分余额查看
6. **打样验收（两周）**：每周统计——
   - **周活**：活跃员工数 / 员工总数 ≥ 60%
   - **完成率**：完成会话（到达 round_complete / 产出落库）/ 发起会话 ≥ 70%
   - **续费意向**：试点末调研（充值续费意向）≥ 80%
   - 未达标 → 复盘培训覆盖、场景包贴合度、定价感知，调整后进入下一轮试点

### 9.2 试点数据核查入口

```bash
# 健康与链路（每日）
curl -s http://127.0.0.1:8080/health && curl -s http://127.0.0.1:9100/health
bash scripts/dev_smoke.sh          # 开发/试运行环境全链路冒烟
# 数据核查（SQLite 直查，只读）：
sqlite3 backend/data/informate.db \
  "SELECT count(*) FROM users;" \
  "SELECT count(*) FROM conversations;" \
  "SELECT count(*) FROM messages WHERE role='assistant';"
```

### 9.3 试点风险预案

| 风险 | 预案 |
|:--|:--|
| 生图余额不足（402） | 火山方舟账户预充值 ≥ 试点预算；监控 `402` 告警 |
| 合规误拦（medical 拦截正常文案） | 用 `PUT /hint` 调发布提示；规则包问题反馈到合规服务迭代（rule_packs） |
| 模型涨价导致单会话成本超售价 | 上线前按第 8 章重算定价；涨价触发时先暂停自动续费，重定价后通知客户 |
| 客户流失（周活 < 60%） | 第 2 周介入：加培训、加场景包示例、免部署费延展试用期 |

---

*本文档随 MVP 迭代更新；版本变更记录：v1（T15 上线准备，2026-08-07）。*
