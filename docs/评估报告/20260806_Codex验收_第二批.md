核验完毕。以下为验收结论。

## 验收结论

**有条件通过**——四份交付物整体扎实、口径与基线基本一致、实验报告源码证据真实可信，但存在一处返工级的前端契约错位（UIUX §6 与 Schema V1 字段不一致）、若干 FR 覆盖缺口与 Schema 校验缺陷，须先修复再进入第三批。

核验口径（简要）：PRD 实际为 46 FR ↔ 46 AC + 18 NFR（已统计）；Schema JSON 解析合法、全部正则可编译、`additionalProperties:false` 与 `if/then/else` 用法符合 draft-07；实验报告引用的 12 处源码行号逐条比对全部命中，P0 判定与方案 a 推荐成立；两个内嵌 YAML 实例逐字段对照 Schema 均通过。

## 问题清单

| # | 严重度 | 交付物 | 问题 | 具体证据 | 修改建议 |
|:--|:--|:--|:--|:--|:--|
| 1 | 高 | UIUX_交互设计_v1.md §6.1/§6.2 | 前端契约与 Schema V1 字段错位：UIUX 引用 `icon`/`display_name`/`suggestions`/`over_round_points`/`max_rounds`/`memory_bank`/`industry_overlay`，Schema V1 实际只有 `emoji`/`name`/`extra_round_points`/`round_limit`/`memory.bank_id_template`/`knowledge`，且 §6.1 的 `memory_bank: informate-tenant_{tenant_id}_{scenario_id}` 是已作废的下划线版（FR-207 定稿连字符版） | UIUX §2.2「来自场景包 `icon`/`display_name`」、§2.3「示例来自场景包 `suggestions` 字段」、§6.2 映射表第 1/2/3/6 行 vs 场景包Schema_v1.json 顶层 13 字段；Schema md §4.5 明确「早期下划线表述作废」 | 以 Schema V1 为准重写 UIUX §6.1/§6.2；若前端确需 `suggestions`/`display_name`，先在 Schema 增字段再双向对齐，删除下划线版 bank 命名 |
| 2 | 高 | UIUX §2.3 vs §4.7；PRD FR-205 vs AC-205 | 50 轮拦截时机自相矛盾：UIUX §2.3「第 50 轮拦截：发送被拦截」、§4.7 表格「第 51 轮发送」触发；PRD FR-205「达 50 轮上限拦截」与 AC-205/§4.2/§4.3「第 51 轮消息被拦截」不一致 | UIUX 两处原文；PRD §2.2 FR-205 与 §6 AC-205、§4.2「单会话 50 轮，触发后拦截」、§4.3「第 51 轮 → 拦截新消息」 | 统一为「第 50 轮正常完成并计费，第 51 轮发送被拦截」，同步修订 PRD FR-205 措辞与 UIUX §2.3 |
| 3 | 中 | 场景包Schema_v1.json compliance | `enabled=true` 时 `rule_packs` 非空并未真正硬校验：then 分支只有 `properties.rule_packs.minItems=1`，缺 `required:["rule_packs"]`，字段缺失时校验通过；Schema md §4.1 却声称「Schema 硬校验」 | JSON `"then":{"properties":{"rule_packs":{"minItems":1}}}`；md §4.1「compliance.rule_packs（enabled=true 时非空，Schema 硬校验）」 | then 分支补 `required:["rule_packs"]`，并加一条校验用例 |
| 4 | 中 | 场景包Schema_v1.json/.md；UIUX | FR-209 发布审查提示无机器可校验载体：compliance 仅 enabled/rule_packs/ai_label，提示文案（后台可配）只出现在实例 A 的 `workflow.description` 与 UIUX 合规提示条，两文档呈现形式不一致（回复附带 vs 常驻提示条） | PRD FR-209「自动附带合规提示…后台可配置提示文案」；Schema compliance properties；实例 A workflow.description；UIUX §2.3 合规提示条 | compliance 增加 `publish_review_hint`（布尔开关+文案，或明确为平台配置项并写入说明），统一呈现形式 |
| 5 | 高 | 场景包Schema_v1.json | 前端渲染缺字段：UIUX 空态 chips 依赖 `suggestions`（Schema 无）；左栏品牌名「行业前缀+基名」依赖按租户展示名机制，Schema 只有全局 `name`（实例 A 直接写「行业工作助手（医美）」把行业混入全局名，跨行业平台不可复用） | UIUX §2.3/§6.2 `suggestions[]`；Schema 顶层 properties 无 suggestions、无 per-deployment display_name；实例 A `name` 字段值 | Schema 增加 `suggestions` 数组；增加部署层 `display_name`（或明确由后端按租户一级行业拼接），并回写 UIUX |
| 6 | 中 | UIUX §2.2 vs PRD FR-602/AC-602 | 工作台「最近消费逐笔记录」界面缺失：FR-602 要求工作台展示余额与最近消费记录，UIUX 仅余额卡+充值弹窗，逐笔记录只在后台积分管理 | PRD FR-602「工作台展示当前余额（积分）与最近消费记录」；UIUX §2.2 余额卡内容 | 在余额卡或充值弹窗增加最近消费列表/入口 |
| 7 | 中 | UIUX vs PRD FR-105 | 员工子账号创建路径冲突：FR-105 要求「主账号可在工作台创建/停用员工子账号」，UIUX 将员工管理放入内部运营后台、主账号工作台仅多充值/后台入口，开放问题 4 未闭环 | PRD FR-105；UIUX §4.2「主账号 vs 员工差异仅三处」、§4.6 员工管理（内部）、§7.2 开放问题 4 | 第三批前决策统一（主账号工作台创建 or 后台导入），回写 PRD/UIUX |
| 8 | 中 | UIUX 头部声明 | 自述覆盖「45 FR ↔ 45 AC + 18 NFR」，与 PRD 实际 46 FR ↔ 46 AC 不符（FR/AC 已全量统计），说明基于旧版 PRD 编写，需按 46 条回查（本文已查出 FR-602、FR-105 两处缺口） | UIUX 第 5 行 vs PRD 全文 `rg 'FR-[0-9]{3}'`=46、`AC-`=46 | 按 46 FR 逐条回查补漏并更正声明 |
| 9 | 低 | 场景包Schema_v1.md §6 | 机器校验脚本引用的两个 YAML 文件（`医美行业工作助手.yaml`/`生成图片.yaml`）在 artifacts/02_design 不存在，按脚本执行直接报 FileNotFoundError（实例仅内嵌在 md） | md §6 代码块文件名；目录 `ls` 无 .yaml | 补齐独立 YAML 文件，或脚本改为从 md 提取 |
| 10 | 低 | Schema md vs UIUX §6.1 | 会话型 `refund_on_failure` 口径不一致：Schema 实例 A/字段说明为 false（已消耗轮次不退），UIUX §6.1 pricing 示例写 true | Schema md 实例 A `refund_on_failure: false` 及 §3.2 说明；UIUX §6.1 `refund_on_failure: true` | 统一为 false |
| 11 | 低 | 场景包Schema_v1.md §4.1 | 条件必填表述混列：`workflow.steps[].tool`、`artifact.actions[].target_scenario` 的「必填」既无 schema if/then 也无独立 B 规则编号（B4/B8 只覆盖取值/存在性），与标注「Schema 硬校验」的项并列，易误导开发 | md §4.1 条件必填清单 vs §4.4 B1-B10 | 补 B11/B12 显式规则或加 if/then |
| 12 | 低 | 场景包Schema_v1.json knowledge.types | v3.1 种子知识量级含「20 案例/10 模板」，`knowledge.types` 枚举只有 terms/faq/scripts/sop/regulations，案例/模板无归属类型 | v3.1 §5.1 量级 vs Schema `knowledge.types` enum | 确认归入 scripts 或扩展枚举 |
| 13 | 低 | api_server_userid实验报告 | 三处表述精度问题：(a) 表 1「api_server.py set_session_vars 签名含 user_id」实为 gateway/session_context.py:206 定义后 import；(b)「rg user_id 仅命中 1 处文档字符串」实为 3203 行 `_session_response` 的 safe_keys 元组（响应序列化字段）；(c)「X-Hermes-Session-Key 只做会话连续性」与源码 docstring「scopes long-term memory」及 SPIKE 报告 M-17「长时记忆作用域通道」表述冲突，准确说法应为「未注入 user 维度，不能作租户隔离」 | session_context.py:206；api_server.py:3203；api_server.py:2046 docstring；Hermes隔离SPIKE报告.md 风险表 | 修正三处表述，结论方向不变 |
| 14 | 低 | api_server_userid实验报告 | Hindsight 端点「POST /v1/default/banks/{bank_id}/memories/recall」未能在插件源码核到（插件经 base_url 客户端调用，端点串未出现）；Scenario 1 检索串库（informate-arch 历史暗号）原因未闭环 | hindsight __init__.py 无 recall 端点字符串；报告 §七 遗留自述 | 补充 Hindsight OpenAPI 出处；按 SPIKE 建议用全新空 bank 复测隔离用例 |

## 必须修复的前 5 项

1. **前端契约双向对齐（#1+#5，返工级）**：UIUX §6 的 `icon`/`display_name`/`suggestions`/`over_round_points`/`max_rounds`/下划线版 bank 命名与 Schema V1 实际字段（`emoji`/`name`/`extra_round_points`/`round_limit`/连字符模板）错位；同时 Schema 补 `suggestions` 与部署层展示名机制，否则前端照 UIUX 开发必然返工。
2. **50 轮拦截时机统一（#2）**：按 AC-205/PRD §4.2「第 50 轮正常完成、第 51 轮发送被拦截」定稿，修订 PRD FR-205 与 UIUX §2.3/§4.7 两处矛盾表述。
3. **合规段 Schema 修正（#3+#4）**：`compliance.rule_packs` 补 `required` 使硬校验声明成立；为 FR-209 发布审查提示增加字段载体（`publish_review_hint` 或明确平台配置归属）。
4. **FR 覆盖缺口闭环（#6+#7+#8）**：补工作台「最近消费记录」、定稿员工子账号创建路径（FR-105 与 UIUX/开放问题 4 冲突）、按 46 FR 回查并更正 UIUX 覆盖声明。
5. **Schema 说明一致性收尾（#9-#12）**：补齐独立 YAML 实例文件、统一 `refund_on_failure` 会话口径、为 tool/target_scenario 条件必填补 B 规则或 if/then、明确案例/模板知识类型归属。

补充判定：实验报告 #13/#14 均为低精度表述问题，不影响「P0 阻塞确认、方案 a（X-Hermes-User-Id 头）合理、落地步骤可执行」三项核心结论——第三批技术方案应直接纳入方案 a 的源码改造任务（`_bind_api_server_session` 单点注入，约 10-20 行）。
